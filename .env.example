DATABASE_URL=

MY_SQL_URL=
MY_SQL_PUBLIC_URL=

# NextAuth JWT signing secret — generate with `openssl rand -base64 33`
AUTH_SECRET=

# Consumed only by scripts/seed-admin.ts, not read at app runtime
ADMIN_EMAIL=
ADMIN_USERNAME=
ADMIN_PASSWORD=
